@model List<Thuchanh2.Models.ProductOrderViewModel>
@{
    ViewData["Title"] = "ProductOrder Details";
    var successMessage = TempData["successMessage"]?.ToString();
    var errorMessage = TempData["errorMessage"]?.ToString();
}
<h3>@ViewData["Title"]</h3>
<hr />

@if (!string.IsNullOrWhiteSpace(successMessage))
{
    <div class="alert alert-success">
        <strong>Success! </strong>@successMessage
    </div>
    <button type="button" class="btn-close float-end" data-bs-dismiss="alert"></button>
}
else if (!string.IsNullOrWhiteSpace(errorMessage))
{
    <div class="alert alert-danger">
        <strong>Error! </strong>@errorMessage
    </div>
    <button type="button" class="btn-close float-end" data-bs-dismiss="alert"></button>
}

<form asp-action="Search" asp-controller="ProductOrders" method="get" class="mb-3">
    <div class="input-group">
        <input type="text" class="form-control" placeholder="Search by Id" name="Id" />
        <button type="submit" class="btn btn-primary">Search</button>
    </div>
</form>

<form>
    <button asp-action="Create" asp-controller="ProductOrders" class="btn btn-primary mb-3">Create Order</button>
    <table class="table table-responsive table-hover table-bordered">
        <thead>
            <tr class="table-active">
                <th>Id</th>
                <th>Quantity</th>
                <th>Product Name</th>
                <th>Order Id</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                @foreach (var productOrder in Model)
                {
                    <tr>
                        <td>@productOrder.Id</td>
                        <td>@productOrder.Quantity</td>
                        <td>@productOrder.Name</td>
                        <td>@productOrder.orderId</td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <a asp-controller="ProductOrders" asp-action="Edit" asp-route-id="@productOrder.Id" class="btn btn-primary">Edit</a>
                                <a asp-controller="ProductOrders" asp-action="Delete" asp-route-id="@productOrder.Id" class="btn btn-danger">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="6">
                        <div>
                            No Order available at this moment!
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</form>
